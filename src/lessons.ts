import type { Config } from '../server/schema';
export type Lesson = { id: string; name: string; short: string; title: string; description: string; concepts: { term: string; text: string }[]; impact: string; tryThis: string; technical: string; source: string; prompt: string; context?: string; config: Partial<Config> };
export const defaults: Config = { mode: 'simulator', instructions: 'Be clear, concise, and helpful. Explain your answer in plain language.', tools: true, memory: true, retrieval: true, planning: false, approvals: true, retry: true, failOnce: false, maxSteps: 6, contextChars: 4000, maxOutputTokens: 1200, expected: '' };
export const lessons: Lesson[] = [
  { id: 'loop', name: 'The agent loop', short: 'How the pieces work together', title: 'A model thinks. A harness runs.', description: 'An agent harness is the software around a model. It supplies context, executes tools, carries results forward, and decides when to pause or stop. The model proposes the next move; the harness makes it happen.', concepts: [{ term: 'Model', text: 'Chooses a response or requests a tool using the information it can see.' }, { term: 'Harness', text: 'Owns the execution loop, available tools, state, permissions, and limits.' }, { term: 'Feedback loop', text: 'A tool result goes back to the model, which can take another step or answer.' }], impact: '“Calculate 24 * 7” needs two model turns: one to request the calculator and one to use its result. A one-turn budget stops before the final answer.', tryThis: 'Run the example. Then set Max model turns to 1 and run again. Compare the stopping point in the trace.', technical: 'The server owns a bounded loop. It appends every Responses API output item, executes validated function calls, and appends a function_call_output with the matching call_id. A response without tool calls ends the loop.', source: 'server/harness.ts', prompt: 'Calculate 24 * 7 and explain how you got the result.', config: {} },
  { id: 'instructions', name: 'Instructions', short: 'Give the agent a role and rules', title: 'Set the direction before the first step.', description: 'Instructions define how the agent should approach a task: its role, tone, priorities, and constraints. They are included with each model request. Clear instructions guide behavior, but software must enforce hard boundaries.', concepts: [{ term: 'Role & task', text: 'Explain the work and what a good answer looks like.' }, { term: 'Instruction priority', text: 'Operator instructions are separate from user requests and reference data.' }, { term: 'Guidance vs. enforcement', text: '“Be brief” is guidance. A tool allowlist is enforced by code.' }], impact: 'The same calculation can produce a normal answer, bullet points, or uppercase text when you change the instructions. In live mode, try a teaching role or a specific audience.', tryThis: 'Run the example with “Use uppercase.” Then change it to “Use bullet points.” and run again.', technical: 'The editable operator instructions are combined with the lab’s base instructions and sent in the Responses API instructions field on every turn. The simulator recognizes uppercase and bullet formatting only; live mode supports arbitrary instructions.', source: 'server/provider.ts', prompt: 'Calculate 24 * 7.', config: { instructions: 'Use uppercase. Include the calculation result.' } },
  { id: 'tools', name: 'Tools & actions', short: 'Turn a request into real work', title: 'Tools give the model something to do.', description: 'A model cannot execute a function just by writing its name. The harness publishes tool schemas, validates a requested call, runs the implementation, and returns the result. The available tool set defines the agent’s reach.', concepts: [{ term: 'Schema', text: 'A contract describing a tool’s name, purpose, and accepted arguments.' }, { term: 'Validation', text: 'Reject unknown tools and malformed arguments before execution.' }, { term: 'Observation', text: 'A structured tool result becomes evidence for the next model turn.' }], impact: 'With the calculator enabled, arithmetic is computed by code. Without it, a live model may answer unaided; the simulator explicitly reports the missing capability.', tryThis: 'Run a calculation, switch General tools off, and run the same request. Open a tool event to inspect its arguments.', technical: 'Tools use strict JSON schemas and server-side Zod validation. The calculator uses a small arithmetic parser, never eval or a shell. General tools controls calculate and create_note; memory, retrieval, and planning have separate switches.', source: 'server/tools.ts', prompt: 'Calculate (180 * 3) + (45 * 2).', config: {} },
  { id: 'context', name: 'Context', short: 'Choose what the model can see', title: 'An agent can only use what reaches it.', description: 'Context is the information included in a model request: instructions, the user’s task, selected memories, and tool results. More text is not automatically better. A harness must choose what to include within a finite budget.', concepts: [{ term: 'Context window', text: 'The model has a finite capacity measured in tokens.' }, { term: 'Selection', text: 'The harness decides which information reaches the model at all.' }, { term: 'Truncation', text: 'Dropping text can also drop the fact needed for the answer.' }], impact: 'A short extra-context limit removes the codeword at the end of the brief. The model cannot reliably recover a fact that was never provided.', tryThis: 'Run with the 100-character limit. Raise Extra context limit to 1000, rerun, and inspect the context event.', technical: 'For a visible teaching example, this lab slices only the extra-context field by characters. This is deliberately not a tokenizer or full context compaction system. The prompt, instructions, memory selection, and accumulated tool outputs have separate bounds.', source: 'server/harness.ts', prompt: 'What codeword appears in the extra context? Use only the supplied context.', context: 'Project brief: We are designing an accessible learning experience for a mixed audience. Keep the explanation practical and show every execution step. The launch codeword is ORBIT.', config: { contextChars: 100 } },
  { id: 'memory', name: 'Persistent memory', short: 'Carry useful facts between runs', title: 'Remembering is a system behavior.', description: 'The model does not update its weights when you ask it to remember. The harness stores a fact outside the model, then includes that fact in future requests. You control what is saved and can remove it from the memory drawer.', concepts: [{ term: 'Working context', text: 'Temporary information available inside a single run.' }, { term: 'Durable memory', text: 'Facts written to disk and retained across new runs and server restarts.' }, { term: 'Recall', text: 'The harness selects saved facts and adds them to the next request.' }], impact: 'Save a preference, start a fresh run, and ask what the agent remembers. Turn memory off and the same fact is no longer supplied, even though it remains on disk.', tryThis: 'Run and approve the memory write. Click “Try recall”, run again, then turn Memory off and repeat.', technical: 'save_memory writes atomically to data/memory-{mode}.json. Simulator and live memory are separate. The latest 20 entries are supplied per run, with a 100-entry store limit. This is simple recency-based recall, not vector search or learned memory.', source: 'server/store.ts', prompt: 'Remember that I prefer concise answers with one concrete example.', config: { memory: true, approvals: true } },
  { id: 'retrieval', name: 'Knowledge retrieval', short: 'Bring the right evidence into view', title: 'Look it up before answering.', description: 'Retrieval gives an agent access to information outside its initial context. The model requests a search, the harness returns matching documents, and the model can ground an answer in that evidence.', concepts: [{ term: 'Knowledge source', text: 'Documents owned by your application, separate from personal memory.' }, { term: 'Retrieval', text: 'Select relevant passages instead of including every document in every request.' }, { term: 'Grounding', text: 'Tie an answer to actual source content and identify the source.' }], impact: 'The included travel policy sets a $180 nightly hotel budget. Without retrieval, that policy is absent from the request; a plausible guess would not be evidence.', tryThis: 'Ask about the hotel budget, then turn Knowledge retrieval off and rerun. Open “Included knowledge” to read the source documents.', technical: 'search_knowledge performs transparent keyword matching over three bundled documents. Production RAG may use embeddings, ranking, access control, and citations. This lab intentionally keeps retrieval small and inspectable.', source: 'server/tools.ts', prompt: 'What is our hotel budget in the travel policy? Cite the source.', config: { retrieval: true, expected: '180' } },
  { id: 'planning', name: 'Planning', short: 'Make the intended work explicit', title: 'A plan is an artifact, not an action.', description: 'Planning turns a request into a short sequence of proposed steps. It can make complex work easier to follow, but recording a plan does not execute it. The harness still needs to run each tool and observe the result.', concepts: [{ term: 'Decomposition', text: 'Break the task into smaller pieces with clear outcomes.' }, { term: 'Visible plan', text: 'A task checklist the user can inspect, not the model’s private reasoning.' }, { term: 'Execution cost', text: 'Planning consumes a model turn and some of the run budget.' }], impact: 'Enable planning for a calculation. The trace gains a make_plan tool call before the calculator, so a two-turn budget can now stop too early.', tryThis: 'Run the example with planning on. Turn it off and compare the number of model turns.', technical: 'Enabling planning adds the make_plan tool and an instruction to call it first. It is model guidance, so live adherence can vary. The tool records up to five steps; this single-agent demo does not delegate to other agents.', source: 'server/harness.ts', prompt: 'Calculate (12 * 8) + 24 and explain the result.', config: { planning: true } },
  { id: 'permissions', name: 'Permissions', short: 'Put a human before a write', title: 'A proposed action can wait for you.', description: 'Permission checks sit between a model’s request and a tool’s execution. The harness can pause, show the exact proposed action, and require a human decision. Approval belongs to the action, not to the wording of a prompt.', concepts: [{ term: 'Read vs. write', text: 'A calculation reads inputs; saving a memory or creating a note changes state.' }, { term: 'Approval gate', text: 'A real pause before execution, controlled by the server.' }, { term: 'Denial feedback', text: 'The model receives a denied result and must continue without that action.' }], impact: 'Creating a note pauses the run. Deny it and no note is created. Approve it and the structured note appears in the trace as a local artifact.', tryThis: 'Run the example, inspect the proposed note, and deny it. Run again and approve it to see the difference.', technical: 'Each approval has a unique ID tied to the active run. Stale decisions are rejected. Approvals expire after two minutes. The only writes are lab memory and local run notes; no email, external actions, or arbitrary filesystem tools exist.', source: 'server/index.ts', prompt: 'Create a note: Review the harness demo with the team on Friday.', config: { approvals: true } },
  { id: 'limits', name: 'Budgets & stopping', short: 'Bound the work and the cost', title: 'Decide what “enough” means.', description: 'An agent can keep asking for tools indefinitely unless the harness imposes a limit. Turn budgets, output limits, cancellation, and timeouts make execution finite. Stopping a run does not roll back actions that already happened.', concepts: [{ term: 'Turn budget', text: 'A maximum number of model requests in one run.' }, { term: 'Output budget', text: 'A per-request limit on generated tokens, including reasoning tokens.' }, { term: 'Cancellation', text: 'Abort future work; completed side effects still exist.' }], impact: 'With one model turn, the calculator can run but the model cannot see its result in a second turn. The run is stopped, not reported as successfully answered.', tryThis: 'Run this one-turn experiment, then increase Max model turns to 3. Live runs also show actual token usage.', technical: 'Model turns are capped at 10; output tokens at 4096 per request. Runs time out after five minutes and support AbortController cancellation. SDK retries can make up to two HTTP attempts per model turn. Token usage is reported when the provider returns it; no dollar estimate is implied.', source: 'server/harness.ts', prompt: 'Calculate 19 * 23.', config: { maxSteps: 1 } },
  { id: 'recovery', name: 'Errors & recovery', short: 'Handle failure without hiding it', title: 'A failed tool need not end the task.', description: 'Tools can fail. A harness must decide which failures are safe to retry, which should return to the model, and which should stop the run. Retrying a write blindly can duplicate an action.', concepts: [{ term: 'Transient failure', text: 'A temporary problem that may succeed on another attempt.' }, { term: 'Bounded retry', text: 'Retry a limited number of times, rather than looping forever.' }, { term: 'Error observation', text: 'Pass a structured failure back so the model can explain or adapt.' }], impact: 'The fault switch makes the first calculator attempt fail. With recovery on, one safe retry succeeds. With it off, the model receives the error.', tryThis: 'Run with the injected fault and recovery enabled, then disable Safe retries and rerun. Find the retry event.', technical: 'Only the injected read-only calculator failure is retried by the tool layer, exactly once. Writes are not automatically retried. OpenAI SDK transient request retries are separately limited to one when recovery is on. Live models may choose a new tool request after seeing an error.', source: 'server/harness.ts', prompt: 'Calculate 144 / 12.', config: { failOnce: true, retry: true } },
  { id: 'evaluation', name: 'Evaluation', short: 'Check behavior against an expectation', title: 'Turn an example into a small test.', description: 'Evaluations compare observable behavior against an explicit expectation. They help detect regressions when instructions, tools, or models change. A useful check must be clear about what it does and does not prove.', concepts: [{ term: 'Test case', text: 'A repeatable request with a defined expected outcome.' }, { term: 'Assertion', text: 'A concrete condition that can pass or fail.' }, { term: 'Coverage', text: 'One passing check says little about tasks it never measured.' }], impact: 'The calculator result is 168. An answer check for “168” should pass; “169” should fail. Neither check proves that all arithmetic or explanations are correct.', tryThis: 'Run with Expected answer contains set to 168. Change it to 169 and rerun.', technical: 'The included evaluator performs a case-insensitive substring check only on completed answers. Stopped, failed, and cancelled runs cannot pass. Production evaluation needs a broader dataset and task-specific grading.', source: 'server/harness.ts', prompt: 'Calculate 24 * 7.', config: { expected: '168' } },
  { id: 'observability', name: 'Traces & state', short: 'Understand what actually happened', title: 'Make the execution inspectable.', description: 'A trace records what the harness did: which context it supplied, which tool was requested, what happened at the permission boundary, and how the run ended. Traces help you distinguish a model choice from a system decision.', concepts: [{ term: 'Trace', text: 'An ordered record of execution events and their data.' }, { term: 'Checkpoint', text: 'Saved progress that survives a server restart.' }, { term: 'Comparison', text: 'Run the same scenario with a different control and inspect the difference.' }], impact: 'A correct final answer can hide a retry or a missing source. Open the trace to see the actual path. Pin a run, change one setting, and compare the outcomes.', tryThis: 'Run the example and expand an event. Pin it for comparison, change a setting, and rerun. Download the trace or revisit it in Run history.', technical: 'Each event checkpoints the run to data/runs.json. The latest 30 runs are retained. Restarted in-flight runs are marked stopped rather than replayed. Export contains prompts, memory snapshots, and results, so treat it as local application data. It does not contain API credentials or private chain of thought.', source: 'server/store.ts', prompt: 'Calculate 8 * 15.', config: { failOnce: true } },
];
